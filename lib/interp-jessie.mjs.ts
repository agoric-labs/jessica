// TODO: Implement Jessie interpreter.

function interpJessie(ast: any, endowments: any, options?: {}) {
    throw makeError('FIXME: Implement');
}
interpJessie.expr = (ast: any, endowments: any, options?: {}) => {
    throw makeError('FIXME: Implement');
};

export default harden(interpJessie);
